import {StyleSheet} from 'react-native';
import ColorConstants from '../../assets/colorContants';

const styles = StyleSheet.create({
  splashHeader: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: ColorConstants.splashBg,
    paddingBottom: '10%',
  },
});
export default styles;
